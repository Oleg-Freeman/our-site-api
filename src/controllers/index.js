const UserController = require('./user.controller');
const PhotoController = require('./photo.controller');
const ComplimentController = require('./compliment.controller');

module.exports = {
    UserController,
    PhotoController,
    ComplimentController
}