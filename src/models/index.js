const UserModel = require('./user.model');
const PhotoModel = require('./photo.model');
const ComplimentModel = require('./compliment.model');

module.exports = {
    UserModel,
    PhotoModel,
    ComplimentModel
}